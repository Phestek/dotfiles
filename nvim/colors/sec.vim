hi clear
if exists('syntax_on')
    syntax reset
endif

let g:colors_name = 'sec'

if(!exists('g:sec_bold'))
    let g:sec_bold = 1
endif
if(!exists('g:sec_italic'))
    let g:sec_italic = 1
endif

let s:palette = {}



